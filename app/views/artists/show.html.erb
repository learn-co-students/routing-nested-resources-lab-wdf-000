<h1><%= link_to @artist.name, artist_songs_path(@artist) %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(song.artist, song) %></li>
  <% end %>
</ul>
